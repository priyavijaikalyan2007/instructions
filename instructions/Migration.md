# Backend Migration

# Frontend Migration

# Database Migration
We will be making the database access layer more structured. Use a proper ORM. The default in Asp.NET Core 10.0 is the built-in ORM called uses Entity Framework Core. While ORMs are powerful, from a code and architecture standpoint, we need to structure them well. 

* Keep SQL statements together in SQL files.